# Audit remediation master fix list — 2026-07-10 (v0.38.0 → 0.39.0/0.40.0)

Archived working document (moved from the repo-root `output/udflow/` scratch tree, 2026-07-11) — the
remediation this file tracked is complete and its outcomes are folded into `CHANGELOG.md`'s `[0.39.0]`/
`[0.40.0]` entries. Kept here as the per-item completion rationale ("done 2026-07-10: ...") that a
CHANGELOG summary doesn't carry.

Source: zero-based full audit delivered 2026-07-10 (4-way scan: docs / prompt core / executable+CI / external research; all key findings spot-verified). This file is the NO-OMISSION backbone for the remediation run. Every item below must end the run as one of: `[x] done` | `[~] done-with-noted-deviation` | `[d] documented-decision-no-change` (with rationale). Nothing may be silently dropped.

Phases ship as separate PRs in order: P0 → P1 → P2 → P3. Version bumps: P0 ⇒ 0.39.0 (behavioral fixes, CHANGELOG entry), P1 ⇒ no bump (record backfill, not run-perceptible), P2 ⇒ 0.40.0 (prompt-layer behavior + structure, CHANGELOG entry), P3 ⇒ no bump (CI-only) unless a shipped file materially changes.

Repo = D:\github\universal-dev-flow-plugin. Paths below are repo-relative. Line numbers verified 2026-07-10 pre-change; re-locate by quoted text if drifted.

---

## P0 — Functional defects (branch: claude/audit-p0-functional)

- [x] **P0-1** *(done 2026-07-10: severity-definitions bullet + underspecified clause + full Non-mutating rule added to the packet block; `:74` tail aligned "(regardless of how ordinary the code looks)"; all five 5k anchors + 5j fields intact, validate-structure green; reviewer-common.md:5 sync-note enumeration updated. `examples/review-packet.md:128`'s illustrative shared-contract copy = `[d]` NOT touched — it self-declares "reconstructed…, not the verbatim packet", and §5 documented-copies already guards those markers; P3-1(d)'s hash guard covers only the two canonical files.)* `udflow/skills/universal-dev-flow/references/review-packet.md:67-81` ("Shared reviewer contract" block — the only copy reviewers receive at runtime) is missing three rules present in canonical `references/reviewer-common.md`:
  (a) the entire **Non-mutating** rule (`reviewer-common.md:17`: do not modify working tree / write or delete files / change git state / run state-changing commands; read-only enforced by instruction since reviewers hold Bash) — only its tail "filter noise, not signal" survived at `review-packet.md:80`;
  (b) `reviewer-common.md:21` "if the task is materially underspecified within the reviewer's discipline, say so explicitly";
  (c) one-line severity definitions for `blocker` / `major` / `minor` (`reviewer-common.md:11-13`) — the block names the labels but never defines them.
  Also align minor wording drift: `reviewer-common.md:33` "...IS a `major` (regardless of how ordinary the code looks)" vs packet `:74` "...IS a `major`." → make identical.
  Fix: add (a)(b)(c) to the packet block, keeping reviewer-common.md canonical; keep both files' "keep in sync" sentences pointing at each other. (P3-1 adds the CI hash guard that makes future drift fail CI.)

- [x] **P0-2** *(done 2026-07-10: all 7 sites updated to "gatekeeper decides + proposes exact entry; MAIN THREAD performs the one serialized write verbatim after the verdict" — gatekeeper.agent.md:86, verification-gate.md:150 + :108, SKILL.md:109, runtime-policy.md:40, implementer.agent.md:47, failure-consolidate.mjs :6-8/:99-100/:115 runtime string (test's three /advisory only|single writer|modifies nothing/i assertions still match); ARCHITECTURE.md:53 checked = executor-neutral ("single-writer invariant holds"), no change needed; repo-wide grep sweep incl. hyphenated form clean outside CHANGELOG history.)* Gatekeeper single-writer contradiction. `udflow/agents/gatekeeper.agent.md:4` `tools: Read, Grep, Glob, Bash` (no Write/Edit) vs `:86` "**You are the single writer.** … perform the one serialized write yourself". Decision (approved): **the main thread performs the one serialized failure-memory write from the gatekeeper's proposed entry** (matches implementer's propose-and-hand-back model; avoids Bash-heredoc writes the layer treats as tripwire). Update ALL FIVE call sites consistently:
  - `udflow/agents/gatekeeper.agent.md:86` (rewrite: gatekeeper proposes the exact final entry text + placement; main thread writes it verbatim after the verdict)
  - `udflow/skills/universal-dev-flow/references/verification-gate.md:150`
  - `udflow/skills/universal-dev-flow/SKILL.md:109` ("the gatekeeper, as single writer, makes the actual edits" → main thread writes from gatekeeper's proposal)
  - `udflow/skills/universal-dev-flow/references/runtime-policy.md:40` (hedged "main thread / gatekeeper" → precise)
  - `udflow/agents/implementer.agent.md:47` (hedged → precise)
  Keep the single-writer *invariant* (exactly one serialized write, after verdict) — only the executor changes.

- [x] **P0-3** *(done 2026-07-10: verification-gate.md:103 fence + SKILL.md:109's failure-retrieve AND failure-consolidate mentions all carry `node ${CLAUDE_PLUGIN_ROOT}/skills/universal-dev-flow/scripts/…`; grep of shipped udflow/ tree: zero bare `skills/universal-dev-flow/scripts/` invocations remain.)* `udflow/skills/universal-dev-flow/references/verification-gate.md:103` bare `node skills/universal-dev-flow/scripts/failure-retrieve.mjs …` → prefix `${CLAUDE_PLUGIN_ROOT}/`. Also fix the prose mention at `SKILL.md:109` (`scripts/failure-retrieve.mjs --query … --log`) to carry the same prefix form. Grep the whole shipped tree for any other bare `skills/universal-dev-flow/scripts/` invocation.

- [x] **P0-4** *(done 2026-07-10, red→green: test-first added `(dd if=/dev/zero of=out.bin bs=1 count=1)` to the plan-gate deny table — confirmed FAILING ('ALLOW' !== 'DENY') pre-fix — plus `(dd if=/dev/zero of=/dev/sda bs=1M)` to the destructive-guard ask table (already green, its anchor had `(`) and the `(dd … of=/dev/null …)` allow counterpart; then plan-gate.js dd anchor class → `(?:^|[\s;&|(])`, dd pattern ONLY (other 7 patterns keep their anchors BY DESIGN); both hooks' dd regexes verified character-identical, so destructive-guard.js's "reused verbatim from plan-gate" comment is now true as written; hooks file all green after.)* dd-of= anchor drift: `udflow/hooks/plan-gate.js:86` anchor class `(?:^|[\s;&|])` vs `udflow/hooks/destructive-guard.js:66` `(?:^|[\s;&|(])` whose comment claims "reused verbatim from plan-gate". Fix: add `(` to plan-gate's anchor class so `(dd if=x of=y)` is caught in plan mode too; verify the destructive-guard comment is then true; add/extend a test covering the parenthesized form for BOTH hooks. (Completes the consolidation.md:98 backlog item — strike it in P1-5.)

- [x] **P0-5** *(done 2026-07-10: contract-guard.js added as 6th `node --check`; SAME COMMIT as the P2-23 zizmor file-level switch so the inserted line can't silently un-suppress the line-pinned ignore; the two §5h-guarded publish-release lines untouched.)* `.github/workflows/validate.yml:48-52` "Syntax-check hooks" lists 5 of 6 hooks → add `node --check udflow/hooks/contract-guard.js`.

- [x] **P0-6** *(done 2026-07-10: (a) `.gitignore` (`*`+`!.gitignore`) force-added/staged — `?? output/` noise gone; (b) copyRepoTree basename filter also skips `output` + `.claude`, and the existing clean-copy control test gained a cheap assertion that the copied tree lacks `output/` — chosen over a new test to avoid another validator spawn.)* (a) `git add -f output/udflow/.gitignore` (content `*` + `!.gitignore`) — per `references/task-contract.md:11` the ignore file itself is committed and travels; ends perpetual `?? output/` status noise. (b) `test/hooks.test.mjs` `copyRepoTree()` (~:984-993) copies the whole working tree including untracked `output/` (~215KB run captures) and `.claude/` into 29 temp trees per run → extend its filter to skip `output` and `.claude` (keep skipping `.git`, `node_modules` as-is). Add a regression assertion if cheap.

P0 verification: `npm test` green; `node --check` all 6 hooks; `node .github/scripts/validate-structure.mjs` green; targeted new tests for P0-4 red→green; `claude plugin validate .` + `claude plugin validate ./udflow` before the 0.39.0 bump; CHANGELOG entry + 3-manifest lockstep bump (plugin.json / marketplace.json metadata+entry / package.json).

---

## P1 — Record backfill (branch: claude/audit-p1-records)

- [x] **P1-1** *(done 2026-07-10: full fixture re-run completed via the committed `fixture-eval.workflow.js` protocol — Workflow run `wf_46b49fed-910`, 21 agents, 0 errors; **5/5 hit recall + 2/2 clean precision** on `claude-fable-5` (reviewer via `model: inherit`; separate judge, same model). baseline.md :5-7 re-dated to 2026-07-10/claude-fable-5, provenance-history line added (prior 2026-06-28/opus-4-8 record preserved; scores held across the 0.32.0→0.38.0 prompt changes AND the model change — no regression); per-fixture table unchanged (all pass; 07's only remark was an explicitly-cosmetic minor note, noted); K=5 section kept as a dated historical record + "not re-run 2026-07-10" marker. `check-model-provenance --model claude-fable-5` → **MATCH**. eval/README.md + RELEASING.md "Model provenance" wording cross-checked — still consistent, no edit needed.)* `eval/baseline.md:5-7` stale (records 2026-06-28 / prompts changed in 0.32.0 f383a0f, 0.35.0 d1c07db, 0.38.0 27cc360). Fix: re-run the committed fixture eval (`eval/fixture-eval.workflow.js` protocol: blind reviewer → independent judge, 7 fixtures) against the CURRENT `udflow:code-reviewer` prompt and record: date, scores (hit recall /5, clean precision /2), model id used for the reviewer runs, and provenance note. If any fixture regresses, that is a P0-class stop-and-investigate before P2 touches prompts further.
- [x] **P1-2** *(done 2026-07-10: row added — "0.38.0 installed / 0.39.0 pre-release tree · Claude Code 2.1.206 · Node 24.16.0 local/20.x CI · Windows 11 · in-session partial verification 2026-07-10" — plus a scope-of-claim footnote listing exactly what was observed: skill engaged (step-8 equivalent); plan-gate live-fired via its `~/.claude/plans/` write-exemption, deny path NOT triggered live (suite-covered); failure-memory correctly no-op'd with no file; destructive-guard/contract-guard/compact-fidelity/orchestration-check not exercised live (green `npm test` is their coverage); clean-profile smoke marked NOT performed — pending. 0.27.x + Copilot rows untouched. Note: the deny-path live observation the audit item hypothesized did not happen this session, so it is recorded as suite-covered, not claimed.)* `docs/compatibility.md` matrix stuck at 0.27.x rows. Add a current row for 0.38.0→0.39.0 reflecting what THIS session can honestly attest: CI structural checks green (3-OS), hooks live-firing observed **in this working session** (plan-gate deny in plan mode, destructive-guard ask, SessionStart failure-memory digest, Stop advisory as applicable) — explicitly NOT a clean-profile install smoke; mark clean-profile smoke as pending/required per release policy. No fabricated claims.
- [x] **P1-3** *(done 2026-07-10: appended a "2026-07-10 — in-session partial verification (NOT the full smoke)" bullet after the kept 2026-06-28 record: CC 2.1.206 / Windows 11, skill activation + plan-gate live-fire via the plans-dir exemption (deny path suite-covered), suite green incl. the newly-added contract-guard syntax check; the full 8-step clean-profile Manual activation smoke explicitly NOT run and flagged as the open pre-/post-release maintainer action; the contract-guard-without-recorded-smoke gap now visible instead of silent. REPAIR 2026-07-10 (P1 panel major M1, gatekeeper-confirmed 3-way via git): contract-guard shipped in **0.33.0 (2026-07-07, commit 00a8975, tag v0.33.0)**, not 0.34.0 as the audit/tasking claimed — RELEASING bullet corrected; root cause = CHANGELOG's missing `## [0.33.0]` heading (panel m3) queued into P2-28.)* `RELEASING.md:177` "Last live-smoked: … 2026-06-28" — 0.34.0 added a NEW hook (contract-guard) without a recorded smoke. Fix: update the record line honestly: add the 2026-07-10 in-session partial verification (which hooks were observed firing, which steps of the 8-step smoke were NOT performed — install/enable/clean-profile steps) and flag the full clean-profile smoke as an open pre-next-release action for the maintainer.
- [~] **P1-4** *(done 2026-07-10 with one noted deviation: Live runs 7–12 appended per the `### Live run` template, prefaced by a batch disclosure (backfilled 2026-07-10 from CHANGELOG + merged-PR record; maintainer dogfood; tier **self-attested** — PR diffs publicly verifiable, verdicts/outcomes self-attested; counts toward volume only; unit=task verified against "What counts": run 7's two udflow arcs inside one release counted ONCE, conservatively). Both explicitly-recorded panel catches quoted (0.36.0 content-loss G1, 0.35.0 drift bug + root-caused 5k CI guard) plus 0.37.0 parity-gap, 0.38.0 name-collision FP, and 0.34.0's --deep ReDoS catch; run 7 also records the ESCAPED dd-anchor drift (verified via `git show e7d5ce3`: destructive-guard changed, plan-gate untouched → closed 0.39.0 P0-4). Run 12 = the P0 run, outcome **pending — PR #86 open**, heading honestly says "live task, outcome pending" not "verified live task". Tally: 6→**12 logged (11 merged; 1 pending)**; publicly-verifiable stays **3** (runs 7–12 counted self-attested); projects stays **2** (no inflation); non-maintainer stays **0**; "near-empty" prose refreshed at the Type-B bullet, tier note, status paragraph, and section intro — volume ≥10 stated as reached-on-paper with the weight caveat; breadth+independence named the binding open gates. DEVIATION: run dates follow the verifiable artifacts — 0.34.0=2026-07-09, 0.35.0/0.36.0=2026-07-10 (commits d1c07db 06:49 / f0df46e 08:29 +08:00, merges + CHANGELOG all 2026-07-10), though the audit tasking listed #82/#83 as 2026-07-09.)* `EVIDENCE.md` (last date 2026-06-29) missing the 0.34.0→0.38.0 dogfood runs. Add Type-B entries per its own "What counts (strict)" rules, sourced from CHANGELOG + merged PRs #80, #83, #84, #85 (+ the 0.35–0.37 entries in CHANGELOG): maintainer dogfood runs of udflow on its own repo; include the two explicitly-recorded panel catches (CHANGELOG.md:103 "the review panel caught a real content-loss bug", CHANGELOG.md:142 "the deep panel caught a real drift bug") and any other panel-caught defect the CHANGELOG entries for 0.34–0.38 record; label honestly as maintainer-run (counts toward volume, NOT toward the "≥1 non-maintainer run" bar).
- [x] **P1-5** *(done 2026-07-10, all statuses grep-verified with file:line: (a) :14 → "6 hooks" / "13 references" (ls-verified: 6 hook .js files, 13 reference .md files). (b) :65 → `[x]` + "Decision (recorded 2026-07-10)": provenance stamp + documented re-test attempt ACCEPTED; mechanism live (`eval/check-model-provenance.mjs` + baseline provenance lines); 2026-07-10 full fixture re-run on claude-fable-5 scored 5/5 + 2/2; criterion closed. (c) destructive-guard parenthesized-POSIX item → DONE: POSIX `(` shipped 0.34.0; the plan-gate `dd of=` sibling half (which 0.34.0 left drifted) closed 0.39.0 P0-4 red→green. (d) backlog statuses: gatekeeper no-fail-first-test-as-blocking-omission SHIPPED 0.27.0 (CHANGELOG "Bidirectional acceptance-criterion ↔ test ↔ change traceability"; gatekeeper.agent.md:61); implementer rollback-steps STILL OPEN (zero rollback matches in implementer.agent.md); spec-reviewer exported-API SHIPPED 0.34.0 B2 (spec-reviewer.agent.md:38); planner-creator vague-AC flag SHIPPED 0.34.0 B3 (planner-creator.agent.md:51) + design.md-presence surfacing SHIPPED 0.25.3 with the agent itself (:21/:50) — audit's "0.37.0/0.25.x" guess resolved to 0.25.3; code↔architecture boundary SHIPPED 0.34.0 B4 (architecture-reviewer.agent.md:35 mirroring code-reviewer.agent.md:52). Parseable-test-id superseded-by-0.38.0 note kept as-is.)* `docs/consolidation.md` status pass: fix stale counts at `:14` ("5 hooks" → 6; "~15 references" → 13); strike/annotate the destructive-guard parenthesized-POSIX backlog item (`:98-99`) as completed (destructive-guard already had `(`; plan-gate closed by P0-4); resolve the `:65` "Decision needed" line with the decision actually taken (provenance stamp + documented re-test attempt accepted; `eval/check-model-provenance.mjs` + baseline provenance lines are the mechanism); re-check remaining backlog items' status (gatekeeper fail-first→pass, implementer rollback steps, spec exported-API check, planner vague-AC flag — note "open" or "shipped in 0.3x" with the version; the parseable-test-id item already marked superseded by 0.38.0 stays).

P1 verification: `node .github/scripts/validate-structure.mjs` green (it greps docs); link check over changed files; no version bump. — *(run 2026-07-10: validate-structure PASS; `check-model-provenance --model claude-fable-5` MATCH; relative link target `eval/fixture-eval.workflow.js` exists; 6 new PR URLs well-formed on the canonical kktu6507 repo (PR #86 confirmed OPEN via `gh pr list`); diff touches exactly the 5 P1 files; manifests untouched — no bump.)*

P1 panel outcome (deep, spec-reviewer + gatekeeper): FIX REQUIRED → repair applied 2026-07-10: M1 RELEASING.md 0.34.0→0.33.0 (git-verified); m2 consolidation.md:65 reworded "closed as-amended" + EVIDENCE.md Track-1 one-line pointer to eval/baseline.md; m3 queued into P2-28. Gatekeeper rerun pending below.

---

## P2 — Structural optimization (branch: claude/audit-p2-structure)

### P2-A Prompt-layer dedup (canonical + one-line pointer; full copies ONLY where runtime isolation requires: agent bodies, packet block)

- [~] **P2-1** *(done 2026-07-10 with one noted deviation: all 8 licensed bullets (+ P2-4's :49/:127, P2-8's :149-150) compressed to trigger + pointer; the 0.27.3 parenthetical deleted entirely; the two mandated `node ${CLAUDE_PLUGIN_ROOT}` invocations at :129/:130 kept runnable. MEASURE: SKILL.md 31,748 → 25,812 bytes = **−5,936 (98.9% of the ≥6,000 target)**. DEVIATION: the last ~64 bytes were not forced — the licensed lines are at their trigger+pointer floor (the two kept command lines alone hold ~700 B) and further cuts would drop sole-copy workflow instructions (usability > byte target). validate-structure run after EVERY SKILL.md edit, green each time; all 5f literals present.)* SKILL.md compression toward pointer-index (~1.5–1.9K tokens): compress `:108` UI-planning bullet (restates external-capabilities.md:56-67 + design-spec.md:40-64), `:109` failure-memory bullet (restates verification-gate.md:91-150), `:130` ratchet bullet (→ command + one clause; canonical = verification-gate *Regression ratchet*), `:144` deep-mode, `:159` fix-class, `:162` Codex, `:165` report-format (restates final-report.md) — each to trigger + pointer. DELETE the release-history parenthetical at `:161` "(It was relocated from `PreCompact` in 0.27.3 — …)". Preserve verbatim all machine literals + the 5f-guarded contract phrases (run validate-structure after EVERY SKILL.md edit).
- [x] **P2-2** *(done 2026-07-10: verification-gate.md:43-49 untouched canonical; SKILL.md:130 per P2-1, command kept; gatekeeper ratchet section cut to its unique judgment rules — classify green→red vs criteria/`mustNotChange`, never auto-suppress without the licensing criterion, no-claim ⇒ no inference — + pointer.)* Regression-ratchet triplication: keep verification-gate.md:43-49 canonical; SKILL.md:130 per P2-1; `gatekeeper.agent.md:49-50` cut to the judgment rules it uniquely owns (classify green→red vs criteria; never auto-suppress) + pointer.
- [x] **P2-3** *(done 2026-07-10: reviewer-selection.md:63 "skips deep mode" → "skips the costlier deep-mode **Tier 2**" with the sentence's later duplicate Tier-2 clause folded in; :65 kept as-is; deep-mode.md:12 + run/SKILL.md:15 same fix; "## Deep Mode" section trimmed to the Evidence-substitution exclusion + pointer. grep "skips?/skip deep mode" in udflow/ = 0.)* `--lite`/Tier semantics contradiction + quintuplication: fix `reviewer-selection.md:63` ("skips deep mode" → "skips the costlier deep-mode **Tier 2**"; keep `:65` Tier-1-still-applies), `deep-mode.md:12`, `udflow/skills/run/SKILL.md:15` — align all to SKILL.md:76's precise wording. Trim `reviewer-selection.md:110-112` "Deep Mode" section to its unique Evidence-substitution exclusion + pointer to deep-mode.md.
- [x] **P2-4** *(done 2026-07-10: all listed sites one-lined to "per `references/app-launch.md` (delegates to `/run`; discloses; tears down only what it started)"; app-launch.md canonical untouched except its own P2-7/P2-14 lines; NOT merged into browser-evidence.)* app-launch preference-order restated 5×: one-line the summaries at `external-capabilities.md:48-50`, `deep-mode.md:48`, `SKILL.md:49,127`, `browser-evidence.md:8,18`, `run/SKILL.md:13` → "per `references/app-launch.md` (delegates to `/run`; discloses; tears down only what it started)". Do NOT merge app-launch into browser-evidence (it covers backend/API launches too).
- [x] **P2-5** *(done 2026-07-10: gatekeeper.agent.md 24,577 → 22,366 bytes (−2,211, incl. the P2-2/P2-6/P2-9 cuts). :84 → "On a Stuck Summary, evaluate whether failure memory must be updated" (:89 keeps the loop rule); Codex pair merged into *Auto-fix loop rules* with a one-line continuity pointer left in *Conflict resolution rules*; :21 + :22-24 tightened into ONE downrank rule + the `{keep, confidence, justification}` record requirement. 5f literals at :96-98 untouched; validate-structure green.)* gatekeeper.agent.md internal dedup (largest agent, 24.3KB≈6.1K tk): iteration-cap stated at `:84` AND `:89` → keep `:89` loop rule, reduce `:84` to "on a Stuck Summary, evaluate failure memory"; Codex-disagreement rule at `:34` AND `:91` → merge into `:91`; `[unverified]`-downrank `:21` + two-stage filter `:22-24` → tighten into one rule + the {keep, confidence, justification} record.
- [x] **P2-6** *(done 2026-07-10: all 27 mentions resolved — each of the 9 files keeps exactly ONE shared-contract mention worded "delivered … in your Review Packet('s "Shared reviewer contract" block)"; per-section repeats reworded to terse back-references ("per the shared admission rule" / "Base output per the shared contract") or deleted (the spec/test "Shared scope discipline" bullets); grep `reviewer-common` in udflow/agents/ = **0**.)* M6: 27 dead runtime pointers — 9 agent bodies cite `references/reviewer-common.md`, unreachable at spawn time (counts: spec 4, test 4, code 4, security 3, architecture 3, operability 3, ui-ux 3, gatekeeper 2, planner-creator 1). Reword to "…shared across reviewers and delivered in your Review Packet's 'Shared reviewer contract' block"; ONE mention per agent, drop per-section repeats.
- [x] **P2-7** *(done 2026-07-10: the 5 copies (plan-grounding.md, browser-evidence.md, app-launch.md, design-spec.md, task-contract.md) replaced with the one-line pointer "Language: per `SKILL.md` *Language And Text Integrity* — user-facing text follows the user's language; technical contracts verbatim"; SKILL.md:85-93 + planner-creator copy kept.)* Language-invariant boilerplate ×7 → keep SKILL.md:85-93 (governs workflow) + planner-creator.agent.md:43 (runtime isolation); delete the copies at plan-grounding.md:68, browser-evidence.md:63, app-launch.md:47, design-spec.md:123, task-contract.md:70-71 (replace with one-line pointer).
- [x] **P2-8** *(done 2026-07-10: SKILL.md ownership pair → one line pointing at review-packet.md *Reviewer-Specific Scope*; the two agent copies + review-packet.md:86-97 kept.)* Reviewer-ownership map ×4 → keep the two agent copies (runtime-justified) + review-packet.md:86-97; one-line SKILL.md:149-150.
- [x] **P2-9** *(done 2026-07-10: gatekeeper :61 halved keeping the blocking-omission judgment + pointer; verification-gate.md:17 canonical untouched; implementer/test-reviewer copies untouched.)* Red→green rule ×4 → verification-gate.md:17 canonical; halve gatekeeper.agent.md:61; keep implementer.agent.md:34 + test-reviewer.agent.md:23 (runtime-justified).
- [~] **P2-10** *(done 2026-07-10, one part deliberately partial: :132 aligned to require exactly the `### Live run` block (now matches :46 + the Stop hook's literal); sentinel-handling stated ONCE — the post-template footer paragraph is canonical, :5 and the Presentation glyph rule now defer to it. PARTIAL `[~]`: the compact/full templates' duplicated Checks/Findings/Verdict/footer blocks kept verbatim for usability — each fence must stay independently copy-paste-emittable, and the 5d/5e guards pin the fence structure (factoring would trade real usability + guard stability for ~150 tk).)* final-report.md internal: align `:132` to require `### Live run` exactly (drop the invented `## Evidence Record` heading requirement — the Stop hook matches only `Live run`); state sentinel-handling once (currently :5, :134, :138); factor the compact/full templates' duplicated Checks/Findings/Verdict/footer blocks (~150 tk) if it does not hurt template usability — else `[d]` with rationale.
- [x] **P2-11** *(done 2026-07-10: reworded exactly as specified; 5j/5k anchors untouched, validate-structure green.)* review-packet.md:9 ambiguous "State the path, not the re-pasted content" → reword: "fill the packet fields from the contract; include the contract's path; do not paste the whole file."
- [x] **P2-12** *(done 2026-07-10: :57/:64/:68 trimmed to "(role split: *Invariants* below)" pointers, each keeping its unique non-role content (descriptive extraction / separate bootstrap pass / pro-max-absent fallback); :120 canonical kept.)* design-spec.md role-split ×4 (:57,:64,:68,:120) → keep :120 (Invariants), trim other three to pointers.
- [x] **P2-13** *(done 2026-07-10: deleted; verified validate-structure's frontmatter check walks only agents/*.md + skills/**/SKILL.md — green; full suite green.)* deep-mode.md:1-4 stray YAML frontmatter (only reference file with one) → delete.
- [x] **P2-14** *(done 2026-07-10: all sites reworded to tool-suffix detection — "the Chrome-extension server (e.g. `mcp__claude-in-chrome__*`)" / "a server exposing `preview_start` (e.g. `mcp__Claude_Browser__*`)" — at browser-evidence.md:15-16, external-capabilities.md:50+:54, app-launch.md:17, deep-mode.md:48 (removed there via the P2-4 one-liner); grep `mcp__Claude_in_Chrome|mcp__Claude_Preview` in udflow/ = **0** (CHANGELOG history exempt).)* MCP server-name literals stale: `mcp__Claude_in_Chrome__*` → current `claude-in-chrome`; `mcp__Claude_Preview__*` → current `Claude_Browser` (`preview_start`). Sites: browser-evidence.md:15-17, external-capabilities.md:54, app-launch.md:17, deep-mode.md:48. Reword to tool-suffix detection ("a server exposing `preview_start`"; "the Chrome-extension server, e.g. `mcp__claude-in-chrome__*`").
- [x] **P2-15** *(done 2026-07-10: final-report.md :143/:162 + doctor/SKILL.md:53 → the issues/new?template=verified-run.yml form URL; final-report.md:171 (the source-of-truth reference) → blob/master file URL; verification-gate.md:106 → "regression-guarded in the source repo's committed oracle + tests" + tree/master/eval/failure-memory URL. grep for remaining relative `](.github/ISSUE_TEMPLATE` in udflow/ = 0.)* Dead-after-install relative links → absolute GitHub URLs (pattern already correct at final-report.md:144): final-report.md:143,162,171; doctor/SKILL.md:53; verification-gate.md:106 (reword eval/test sentence to "regression-guarded in the source repo" + URL).
- [d] **P2-16** *(decided 2026-07-10, no code change: (a) never-hard-dependency ×11 KEPT as belt-and-braces — checked each Invariants occurrence: all are already one-liners, so appending the protocol citation would LENGTHEN, not shorten; no citations added; (b) reviewer section skeleton kept (not duplication); (c) per-reference applied Detect→Use→Else-Disclose sections kept — 2026-06-28 conclusion re-verified.)* `[d]`-candidates (address by documented decision, change only if cheap+safe): never-hard-dependency restated ×11 (belt-and-braces Invariants; my call: KEEP, add "(per the Detect→Use→Else-Disclose protocol, external-capabilities.md)" citation where a full re-derivation exists); the 7 reviewers' shared section skeleton (NOT duplication — domain-specific content); per-reference Detect→Use→Else-Disclose applied sections (2026-06-28 audit conclusion re-verified: applied protocol ≠ copy).

### P2-B Dead weight

- [x] **P2-17** *(done 2026-07-10: export deleted + the false ":91 Exposed via _internal for the CLI" comment corrected to "called directly by main()"; zero importers re-verified by grep before AND after (repo-wide `_internal` = 0); function kept; node --check green.)* `udflow/skills/universal-dev-flow/scripts/contract-check.mjs:99` delete dead `export const _internal` (zero importers; `main()` calls `changedPathsFromGit` directly at :111) + fix the false `:91` comment "Exposed via _internal for the CLI".
- [x] **P2-18** *(done 2026-07-10: FOLDED — const deleted, the regex test inlined into the debug string so the `reportsBlock=` signal survives; no behavior change; suite green.)* `udflow/hooks/orchestration-check.js:302` `finalReportsBlock` computed per Stop event, feeds only the `:331` debug line (pre-0.11.0 F3 vestige) → fold into the debug expression or delete.
- [x] **P2-19** *(done 2026-07-10: `export` dropped; grep before+after confirms only internal uses (:87/:289) and hooks.test.mjs imports only createDeterministicPluginArchive / defaultBytesRunner / defaultRunner / runRelease; node --check + suite green.)* `.github/scripts/publish-release.mjs:74` `classifyReleaseViewFailure` exported but imported nowhere (used internally :87,:289) → drop `export` keyword.
- [x] **P2-20** *(done 2026-07-10: linked from verification-gate.md's Entry Template section ("A filled-in example … lives at `examples/FAILURE_MEMORY.sample.md` in the shipped plugin") AND from all three READMEs' Docs lists — same commit, same list position (after external-capabilities), translated line each; §8 parity green.)* `udflow/examples/FAILURE_MEMORY.sample.md` shipped but referenced by nothing current. Decision: LINK it (good template incl. retire-marker doc) from verification-gate.md failure-memory section + README Docs list; else stop shipping. Implement the link option.
- [d] **P2-21** *(decided 2026-07-10, KEEP — the audit's removal hypothesis is REFUTED: verified same day against the official hooks reference (code.claude.com/docs/en/hooks, fetched 2026-07-10): `PostToolBatch` EXISTS ("After a full batch of parallel tool calls resolves, before the next model call") and accepts `hookSpecificOutput.additionalContext`. Literal left untouched in HSO_ACCEPT_EVENTS. No code change.)* `.github/scripts/validate-structure.mjs:277` `HSO_ACCEPT_EVENTS` contains `"PostToolBatch"` — no such Claude Code hook event; weakens 5g guard → verify against current CC hooks docs, then remove.
- [x] **P2-22** *(done 2026-07-10: engines.node ">=20" added; description → "…Tests cover the session hooks, the release publisher, the CI structure validator, and the workflow scripts."; version stays 0.39.0 — P2b bumps; validate-structure §3d green.)* `package.json`: add `"engines": { "node": ">=20" }`; refresh stale description ("Tests cover the session hooks" → tests also cover release publisher, CI validator, 5 scripts).
- [x] **P2-23** *(pulled-forward-DONE in P0-5's commit, 2026-07-10: switched to file-level `- validate.yml` ignore; comment annotates that file-level was chosen because a `:line` pin silently un-suppresses on any line shift — required because P0-5 inserts a line above :67.)* `.github/zizmor.yml:15` line-pinned ignore `validate.yml:67` (silently un-suppresses on any edit above) → switch to file-level ignore for `adhoc-packages` on validate.yml, or annotate the pin as accepted-fragile with a maintenance note. Prefer file-level.

### P2-C Hooks infra (documented copies + drift sync — NO shared lib, preserving per-hook failure isolation)

- [x] **P2-24** *(done 2026-07-10: `MAX_STDIN = 5 * 1024 * 1024` added (plan-gate constant style) + the rawBytes read-loop cap with debug("stdin over cap; skipping") + pause + exit 0; the 5s watchdog and the separate 32MB transcript cap kept; node --check + suite green.)* Sync drift #1: `udflow/hooks/orchestration-check.js:194-198` stdin reader has NO `MAX_STDIN` byte cap (other five have it) → add the cap, same constant/style.
- [x] **P2-25** *(done 2026-07-10: normalized — `preserveDisabledForProject(input)`, call site passes the parsed event; root precedence byte-identical to before (CLAUDE_PROJECT_DIR → event cwd → process.cwd()) — the process.cwd() fallback is kept as a documented deliberate extra (SessionStart events may lack cwd; in-function comment). compact-fidelity tests green UNMODIFIED, incl. the event-cwd opt-out pin. Known micro-staleness: hooks.test.mjs:2740's descriptive COMMENT quotes the old caller line — test file deliberately untouched (P2-27/P2b owns it), the behavior it describes is unchanged.)* Sync drift #2: `compact-fidelity.js:82` settings-flag reader takes a `cwd` string while the other three (`plan-gate.js:118`, `destructive-guard.js:101`, `contract-guard.js:212`) take the event object → normalize signature (behavior-neutral), or `[d]` with a sync-comment if normalization risks regression.
- [x] **P2-26** *(done 2026-07-10: stamped — debug() ×6, stdin-reader ×6 (incl. the P2-24'd orchestration-check), settings-flag reader ×4, neutralize() ×2, quote-stripper ×2 — each a one-line "kept in sync with <all siblings> (documented copy — see P3 garden hash guard)"; dd-of= ×2 verified already cross-commented (plan-gate "kept character-identical with destructive-guard.js" ⇄ destructive-guard "reused verbatim from plan-gate") — no change. node --check ×6 green.)* Stamp every copied infra cluster with sync-comments naming all sibling copies: `debug()` ×6, stdin-reader ×6 (after P2-24), settings-flag reader ×4, `neutralize()` ×2 (load-failure-memory.js:119 / compact-fidelity.js:53), quote-stripper regex ×2 (plan-gate.js:72 / destructive-guard.js:43), dd-of= pattern ×2 (post-P0-4 identical). Machine enforcement comes in P3-1.

### P2-D Test & changelog structure

- [x] **P2-27** *(done 2026-07-10: split into `test/helpers.mjs` (shared infra, bodies byte-preserved, single export block) + 5 per-subject files — plan-gate-guards 78 / orchestration-check 82 / session-memory-hooks 40 / release-publisher 20 / validate-structure 28 = 248 runtime tests vs the monolith's 247 (+1 = the sanctioned trio parent counting its 3 subtest rows); name-accounting: 244 names verbatim in place + the 3 trio names initially re-composed (parent+row) — P2 panel flagged the packet's "247/247 verbatim" claim as overstated by 3; POST-PANEL REPAIR restored all 3 original full strings verbatim as the subtest rowNames (247/247 verbatim now true again; --test-name-pattern continuity restored). Suite-total accounting corrected per panel: 367 = 247 moved + 1 trio parent + 118 pre-existing + 1 helpers.mjs phantom (bare `node --test` discovers every .mjs under test/, so helpers.mjs counts as one contentless pass — its header comment now says so truthfully; excluding it would need quoted globs = Node ≥21, CI floor is 20 → accepted + documented). The compact-fidelity opt-out tests are "table-converted, name/assertion-preserving", not "unmodified" (record wording corrected per panel). Table conversions ONLY the sanctioned ones: the 4 per-hook opt-out suites → for-of row tables (each row keeps its original test name + assertion message; count-neutral), and the :2304/:2314/:2322 trio → ONE table-driven test with 3 `t.test` rows, distinct messages preserved. `hooks.test.mjs` deleted same commit; `validate.yml` zero-tests guard → 6-file for-loop (comment kept); the 4 publish-release import names verified importing cleanly; PreCompact pins moved verbatim (grep-confirmed both in session-memory-hooks.test.mjs). Repo grep for `hooks.test.mjs`: remaining mentions = CHANGELOG history, the new files' own provenance headers, and `examples/review-packet.md:73` — `[d]` NOT updated: it sits inside a commit-pinned `git show f8bef45 --` reconstruction command whose paths exist only at that commit (rewriting would break the historical command; same exemption class as CHANGELOG history). Full suite: baseline 365 tests/360 pass/0 fail/5 skip @ 41.5s → post-split ×2 STABLE 367/362/0/5 @ 17.1s + 16.6s (totals ≥ baseline; the ±1 beyond my +1 ledger is hooks-portability.test.mjs's spawnSync shell-probe registration noise under parallel load — pre-existing, file untouched). One licensed comment-only fix: the event-cwd pin's stale compact-fidelity caller quote updated per P2-25's explicit hand-off note.)* Split `test/hooks.test.mjs` (3,209 lines / 334 tests / one serialized `node --test` child) into ~5 per-subject files + shared `test/helpers.mjs` (spawn-hook-with-stdin-JSON, `mkTranscript`, `copyRepoTree`, release-runner utils): orchestration-check / plan-gate+destructive-guard+contract-guard / load-failure-memory+compact-fidelity / release-publisher (~:1196-1673 incl. `makeReleaseRunner`+`parseTar`) / validate-structure negatives (~:978-1194, :1674-1817). SAME COMMIT: update `.github/workflows/validate.yml:57` `test -f test/hooks.test.mjs` zero-tests guard to the new file set. Convert the obvious table-driven candidates (per-hook opt-out suites; "not exemptible" trio :2294/:2304/:2312). Preserve the PreCompact-absence regression pins (~:2770). Test count must not drop (334 baseline); wall-clock should improve via per-file parallelism.
- [x] **P2-28** *(done 2026-07-10: (1) missing `## [0.33.0] - 2026-07-07` heading inserted above the orphaned `### Added` block (net-0 lines — the middle of the 3 stray blanks became the heading); pre-archive headings verified 0.34.0:216 → 0.33.0:287 → 0.32.0:301, and "Version bumped 0.32.0 → 0.33.0" now sits under 0.33.0. (2) archive: `docs/changelogs/CHANGELOG-0.x.md` = 4-line header ("Archived… 0.0.x through 0.29.0. Current changelog: ../../CHANGELOG.md", byte-preserved note) + the 931 moved lines (`## [0.29.0]`→EOF) = 935 lines; CHANGELOG.md 1262 → 334 lines. Reconciliation: 931 moved + 331 remaining = 1262 original ✔; new lines = 3 tail (`---` + blank + "Older releases (0.0.x – 0.29.0): see …" link) + 4 archive-header. (3) RELEASING.md "When to bump the version" gains the going-forward grouped-bullets-not-narratives note + archive pointer. (4) validate-structure PASS post-archive (§3c satisfied — current version's heading stays in the main file); `node --check publish-release.mjs` green untouched; `releaseNotes()` grep-confirmed to read ONLY the `## [<version>]`→next-heading slice of CHANGELOG.md, so the archive is semantics-neutral. PRE-EXISTING quirk found while confirming, NOT touched (out of scope, recommendation logged): `releaseNotes()` exact-matches the UNDATED `## [x.y.z]` heading form, but headings have carried ` - <date>` suffixes since 0.28.0 — releases have therefore used the `See CHANGELOG.md for vX.` fallback notes since long before this change (verified identical against pre-change HEAD).)* CHANGELOG archive (Node.js pattern, nodejs/node#6337): keep 0.30.0+ entries in `CHANGELOG.md`; move 0.0.x–0.2x to `docs/changelogs/CHANGELOG-0.x.md`; add an index/link line at the main file's tail. Must keep validate-structure §3 (top-entry version parity) green. Going-forward note in RELEASING.md: entries use keep-a-changelog grouped bullets, not narratives. *(ADDED from P1 panel m3, pre-existing defect: insert the MISSING `## [0.33.0] - 2026-07-07` heading above contract-guard's "### Added" block — currently orphaned under the 0.34.0 heading (headings jump :216→:301), which caused the P1 misattribution; no bump involved.)*
- [d] **P2-29** *(decided 2026-07-10, documented — no adoption: CONTRIBUTING.md "Local development" gains a "Testing notes" line recording that `t.assert.snapshot` was evaluated 2026-07-10 and deferred (flag-free only on Node ≥ 23.4; CI pins Node 20; revisit when the CI floor moves, ref this item).)* Snapshot-testing decision: node:test `t.assert.snapshot` is flag-free only ≥ v23.4; CI pins Node 20 → DEFER adoption; record as a documented follow-up (where: eval/README or CONTRIBUTING testing note) so the idea isn't lost. `[d]` expected.

P2 verification: `npm test` green with count ≥ 334 + timing comparison; validate-structure green after every prompt edit (5d/5f literal guards); `claude plugin validate` both dirs; grep-verify zero remaining references to deleted/renamed things; CHANGELOG entry + lockstep bump 0.40.0. — *(P2b run 2026-07-10: full `npm test` ×2 post-split = 367 tests / 362 pass / 0 fail / 5 skip, stable, 17.1s + 16.6s vs 41.5s baseline (365/360/0/5); split-file subtotal 248 ≥ 334-clause equivalent (the hooks-group tests all preserved — 247 names accounted 1:1 + trio parent); validate-structure PASS post-archive and post-bump; `node --check` ×6 hooks + publish-release green; `claude plugin validate .` and `./udflow` both "Validation passed" AFTER the 0.40.0 lockstep bump (plugin.json / marketplace.json metadata+entry / package.json + CHANGELOG `## [0.40.0]` grouped-bullets entry); grep sweep for `hooks.test.mjs` clean outside CHANGELOG history / new-file provenance headers / the `[d]`-exempted commit-pinned `git show` in examples/review-packet.md:73.)*

---

## P3 — Tooling (branch: claude/audit-p3-tooling)

- [x] **P3-1** *(REPAIRED 2026-07-11 post-panel: the P3 deep panel's M1 (3/3 verified, gatekeeper 6-way confirmed) caught this item marked [x] while SILENTLY DROPPING the stdin-reader ×6 cluster from §9d — six shipped hook stamps pointed at a guard that did not exist, and load-failure-memory was the one hook of six with no over-cap test. Repair: §9d gains the STDIN_MARKER presence check across all 6 hooks (mirrors the settings-flag pattern; stamps now truthful), a 4th 9d negative-test row proves marker-loss → named failure, and load-failure-memory gains a DISCRIMINATING over-cap test (control digest under-cap, silence over-cap). Panel m1 also fixed: RIGOR_ANCHORS now pins the `major` definition ("should be fixed before the work is considered ready") alongside blocker/minor. Panel m2 fixed: the 9c negative's overshoot is size-independent (64 KB). Panel m3 = [d]: the eval-manifest parser's indentation tolerance is accepted — bounded impact (merges can only lose entries, and any loss fails the parity assertions loudly), header mandates the flat grammar.)* *(original completion 2026-07-11, commit 9286bae: validate-structure §9 "garden" — 9a reference reachability (13/13); 9b agents⇄plugin.json parity both directions; 9c size caps SKILL 30,000 B / gatekeeper 27,000 B with conscious-override message; 9d copy-sync: dd regex strictEqual (P0 panel M2 CLOSED), debug()×6+neutralize()×2 body identity (one deterministic normalization: each hook's own basename in log labels → `<hook>`), quote-stripper pair strictEqual, settings-flag readers by precedence-marker presence only; all extractions FAIL CLOSED with re-point messages; 9e plugin-root lint narrowed to the `node <path>` invocation form; (d3) shipped as 4 new §5k RIGOR_ANCHORS over the two canonical files only. FIVE negative tests, each asserting the specific garden message.)* "Garden" dead-weight + sync guard (new validate-structure section(s), borrowing wshobson/agents `make garden`): (a) every `references/*.md` reachable from SKILL.md; (b) every agent in plugin.json exists & vice versa; (c) size caps w/ headroom: SKILL.md, gatekeeper.agent.md (fail on regression past agreed cap); (d) copy-sync hash checks: reviewer-common.md shared-contract ⇄ review-packet.md block (normalized), the 6-hook infra clusters (P2-26), dd-of= regex pair *(P0 panel finding M2 folds in here: extract the dd regex literal from BOTH hook sources and strictEqual them — turn the "character-identical" comment into an enforced invariant; hash-check scope stays the two canonical files only, examples/review-packet.md:128 excluded)*; (e) `${CLAUDE_PLUGIN_ROOT}` lint: no bare `node skills/universal-dev-flow/scripts/` in shipped .md files. Negative tests for each new check in the validate-structure test file.
- [~] **P3-2** *(shipped 2026-07-11: link-check.yml, lychee-action SHA e7477775783ea5526144ba13e8db5eec57747ce8 = v2.9.0, weekly cron+dispatch+PR-on-md, contents:read, REQUIRED job; .lycheeignore seeds localhost/example/mailto/issues-new deep-links. Deviations: no cache step (weekly cadence), no --include-fragments (anchor FP class). lychee not runnable locally → first-run findings fixed on the P3 PR = enforcement point.)* lychee link-check workflow (lycheeverse/lychee-action, SHA-pinned, weekly schedule + manual dispatch; cache; excludes for localhost/example domains). Fix or allowlist whatever it finds on first run.
- [~] **P3-3** *(shipped 2026-07-11: typos.yml, crate-ci/typos SHA bee27e3a4fd1ea2111cf90ab89cd076c870fce14 = v1.48.0, push+PR, contents:read. DEVIATION: config is `typos.toml` NOT `_typos.toml` — §6 scanScratch forbids leading-underscore committed files; typos reads typos.toml from the same chain. extend-exclude zh-TW/ja READMEs + docs/changelogs/; conservative identity extend-words. Zero-findings baseline pending first CI run on the P3 PR.)* typos-cli workflow (crate-ci/typos, SHA-pinned) on PR/push; seed `_typos.toml` excluding zh-TW/ja READMEs' romanized terms if needed; zero-findings baseline before merge.
- [x] **P3-4** *(done 2026-07-11: flag exists on CLI 2.1.206 ("Treat warnings as errors"); `--strict .` and `--strict ./udflow` both pass locally incl. after $schema; validate.yml upgraded to --strict on both invocations KEEPING the best-effort headless guard + fallback echoes; comment records the local verification.)* Upgrade CI `claude plugin validate` from best-effort to `--strict` where feasible; keep the documented fallback if the CLI can't run headless (record actual behavior; never fake a pass).
- [x] **P3-5** *(done 2026-07-11: $schema added as FIRST key; validate-structure green; plugin validate green plain AND --strict; version stays 0.40.0.)* `udflow/.claude-plugin/plugin.json`: add `"$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json"` (editor validation; verify validate-structure tolerates unknown key and `claude plugin validate` accepts it).
- [x] **P3-6** *(done 2026-07-11: eval/manifest.yaml (7 flat entries, id/file/lang/expected/scorer + policy header) + test/eval-manifest.test.mjs three-way parity (manifest ⇄ disk ⇄ FIXTURES array regex-extracted from the workflow script, with the why-comment: Workflow runtime can't read fs, test enforces the hand-sync) + frontmatter ground-truth cross-check; red-proofed live via ghost entry (2 failures → revert → 3/3); eval/README "Manifest + assertion policy": deterministic-first, LLM-judged never enters the ratchet.)* eval manifest (promptfoo/openai-evals shape): `eval/manifest.yaml` (id, description, fixture path, expected, scorer) as the single machine-readable index consumed/validated by a small check (fixture files ⇄ manifest parity — replaces the hand-synced list in fixture-eval.workflow.js or validates it); document in eval/README the deterministic-first assertion policy ("LLM-judged checks never enter the ratchet").
- [~] **P3-7** *(self-checked 2026-07-11 — pipx/uvx absent locally: static check of both new workflows against zizmor 1.26.1 rule classes (40-hex SHA pins + version comments, contents:read, persist-credentials:false, ZERO run: blocks → no injection surface, plain pull_request trigger, no adhoc installs); enforcement = existing zizmor.yml job on the P3 PR.)* zizmor conformance for all NEW workflows (P3-2/P3-3): SHA-pinned actions, least-privilege `permissions:`, no template injection; zizmor job green.
- [x] **P3-8** *(done 2026-07-11, red→green TEST FIRST via the existing makeReleaseRunner harness asserting on the relnotes.md handed to `gh release create --notes-file`: RED verbatim = "a `## [x.y.z] - YYYY-MM-DD` heading must yield the section body, not the see-CHANGELOG fallback / actual: 'See CHANGELOG.md for v1.2.3.'" — proving every release since 0.28.0 published the fallback. FIX: exact === heading match → anchored regex `^## \[<escaped>\](?: - .*)?\s*$`; collect loop verified unchanged; fallback test for missing headings kept green; 22/22 in the file. Next real release (0.40.0 dated heading) now gets real notes.)* *(ADDED 2026-07-10 from P2b's reported-not-fixed finding)* `.github/scripts/publish-release.mjs:93` `releaseNotes()` exact-matches the undated `## [x.y.z]` heading, but CHANGELOG headings carry ` - <date>` since 0.28.0 → every release since has published the "See CHANGELOG.md for vX." fallback instead of real notes. Fix: tolerate the dated form in the heading match; add a regression test in test/release-publisher.test.mjs (dated heading → real notes extracted; missing heading → fallback). Behavioral publisher change — needs its own red→green test.

P3 verification: full CI matrix green incl. new jobs; validate-structure negative tests red→green; no bump unless a shipped file changed materially (then fold into 0.40.0 if P2 not yet merged, else 0.40.1). — *(run 2026-07-11 in the P3 worktree: baseline 367/362/0/5 @17.4s; after ×2 = **377/372/0/5** stable @ ~18s (+10 = exactly the new tests: 5 garden negatives + 2 release-notes + 3 eval-manifest); validate-structure green untouched+after every change; 5 garden negatives prove injected-defect→named-failure; node --check ×8 green; plugin validate ×4 green (plain+--strict × both dirs); version grep all 0.40.0, no new CHANGELOG section. Full-CI-matrix green (lychee/typos first runs + zizmor over the new workflows) is pending the P3 PR by design — named enforcement point of the three [~] items.)*

---

## Global guards (apply to every phase)

- **E-list — verified-clean, DO NOT "fix":** 3 READMEs are in sync (CI-enforced trilingual parity — preserve that check when touching validate-structure); no orphan docs/examples/references; `.mcp.json`+`mcp.example.json` pair intentional; root+udflow LICENSE duplication intentional; marketplace vs plugin description difference intentional; action pins current; hooks.json has no dead entries; all script CLI flags consumed; agent section skeleton ≠ duplication.
- **Machine literals verbatim everywhere:** `READY`/`FIX REQUIRED`/`NOT READY`, `blocker`/`major`/`minor`, `udflow:verify=`/`udflow:delivery=`/`udflow:panel=` tokens, opt-out keys, `### Live run`.
- **Three-manifest lockstep on every bump** + matching CHANGELOG top section; run `claude plugin validate .` AND `claude plugin validate ./udflow` locally before each bump.
- **README parity:** any README.md content change must be mirrored to README.zh-TW.md + README.ja.md in the same commit (CI enforces structure parity).
- **Docs↔code alignment sweep at end of each phase:** grep for the old wording after each rename/removal (e.g. after P2-27, no doc may still say "hooks.test.mjs" as the single test file; after P0-2, no file may still call gatekeeper the write executor).
- **Honesty rule for P1:** no fabricated smoke/run claims; in-session partial evidence labeled as such; gaps stated as gaps.
