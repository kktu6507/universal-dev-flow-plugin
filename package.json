{
  "name": "udflow",
  "private": true,
  "version": "0.42.0",
  "description": "Universal Dev Flow — Claude Code plugin. Tests cover the session hooks, the release publisher, the CI structure validator, and the workflow scripts.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test"
  }
}
